var str = "usman"; // inference mean variable will intialize the dataType which is declared first
str = "only string can be assigned";
var num = 12; // first dataType is intialize will be the inference;
num = 3; // only number data;
var bool = true;
bool = false; // only boolean datatype
var anyType; // if the variable has no inference it will be consider as any datatype mean any dataType can be assigned;
anyType = 12;
anyType = "any dataType";

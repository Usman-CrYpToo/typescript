let str = "usman"; // inference mean variable will intialize the dataType which is declared first
str = "only string can be assigned"; 

let num = 12; // first dataType is intialize will be the inference;
num = 3; // only number data;

let bool = true;
bool = false ; // only boolean datatype

let anyType; // if the variable has no inference it will be consider as any datatype mean any dataType can be assigned;
anyType = 12;
anyType ="any dataType";

let constInference:4; //similar to const now the variable is only assigable to 4 number not any other number;
constInference = 4;